import Button from './components/Button';

const App = () => {
    return (
        <div>
            Hello, React with TypeScript!!
            <Button>Click</Button>
        </div>
    );
};

export default App;
